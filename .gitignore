.claude/settings.local.json

# Retrato de onde o trabalho parou, reescrito a cada turno pelo hook Stop
# (scripts/estado.js). Versionar faria a arvore ficar suja em todo turno,
# brigando com o fluxo de commit que ele existe para proteger. O status
# compartilhavel e o backlog de issues no GitHub.
.claude/estado.md
.claude/estado-nota.txt

# Licenca e ultimo encerramento da janela de trabalho (scripts/janela.js).
# Estado de relogio da maquina, nao do projeto.
.claude/janela.json
